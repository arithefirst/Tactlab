services:
  # Main service
  tactlab:
    container_name: tactlab-main
    image: ghcr.io/arithefirst/tactlab
    environment:
      - NODE_ENV=production
      - S3_ENDPOINT=127.0.0.1

      # Change These
      - S3_ACCESSKEY=minioadmin
      - S3_SECRETKEY=minioadmin
      - S3_PORT=9000
      - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
      - CLERK_SECRET_KEY=your_clerk_secret_key_here
      - TL_API_KEY=your_tactlab_api_key_here
      - TL_INDEX_ID=your_tactlab_index_id_here
      - DATABASE_URL=postgres://<user>:<password>@postgres:5432/tactlab
    ports:
      - 3000:3000
    depends_on:
      - postgres

  # PostgreSQL
  postgres:
    container_name: tactlab-postgres
    image: postgres:15
    restart: always
    environment:
      - POSTGRES_DB=tactlab

      # Change These
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - 5432:5432
    volumes:
      - tactlab-db:/var/lib/postgresql/data

volumes:
  tactlab:
  tactlab-db:
